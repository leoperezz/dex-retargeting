retargeting:
  type: vector
  urdf_path: dex5/Dex5-URDF-R.urdf

  # Target refers to the retargeting target, which is the robot hand
  # Finger 1=thumb (Link_14R), 2=index (Link_24R), 3=middle (Link_34R), 4=ring (Link_44R), 5=pinky (Link_54R)
  target_origin_link_names: [ "base_link00", "base_link00", "base_link00", "base_link00", "base_link00" ]
  target_task_link_names: [ "Link_14R", "Link_24R", "Link_34R", "Link_44R", "Link_54R" ]
  scaling_factor: 1.5

  # Source refers to the retargeting input, which usually corresponds to the human hand
  # MediaPipe indices: 0=wrist, 4=thumb tip, 8=index tip, 12=middle tip, 16=ring tip, 20=pinky tip
  target_link_human_indices: [ [ 0, 0, 0, 0, 0 ], [ 4, 8, 12, 16, 20 ] ]

  # A smaller alpha means stronger filtering, i.e. more smooth but also larger latency
  low_pass_alpha: 0.2
